import * as React from 'karet';

/**
 * @param {Props} props
 */
const App = ({ state }) => {
  return (
    <main>
      App
    </main>
  );
};

export default App;

//

/**
 * @typedef {object} Props
 * @prop {any} state
 */
